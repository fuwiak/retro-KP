# Railway Variables Setup Guide

## ✅ Co masz w Railway (Shared Variables):
- `AMOCRM_ACCESS_TOKEN` ✓
- `AMOCRM_AUTHORIZATION_CODE` ✓
- `AMOCRM_CLIENT_ID` ✓
- `AMOCRM_CLIENT_SECRET` ✓
- `AMOCRM_REDIRECT_URI` ✓
- `AMOCRM_SUBDOMAIN` ✓
- `GROQ_API_KEY` ✓
- `IMAP_PASSWORD` ✓
- `IMAP_PORT` ✓
- `IMAP_SERVER` ✓
- `IMAP_USER` ✓

## ⚠️ Co trzeba dodać w Railway:

### Wymagane (dla podstawowej pracy):
1. **`AMO_PIPELINE_ID`** - ID воронки в amoCRM (число)
2. **`AMO_LEAD_STATUS_ID`** - ID начального статуса сделки (число)
3. **`AMO_RESPONSIBLE_USER_ID`** - ID ответственного пользователя (число)
4. **`IMAP_FOLDER`** - папка для чтения писем (по умолчанию "INBOX", можно не добавлять)

### Опциональные (для полной функциональности):
5. **`AMO_REFRESH_TOKEN`** - токен обновления (если есть)
6. **`AMO_PIPELINE_SALES_ID`** - ID воронки "Продажи"
7. **`AMO_PIPELINE_NKU_ID`** - ID воронки "НКУ"
8. **`AMO_PIPELINE_SERVICES_ID`** - ID воронки "Услуги"
9. **`AMO_CP_SENT_STATUS_ID`** - ID статуса "КП отправлено"

### 1C Integration (если используется):
10. **`ONEC_BASE_URL`** - URL 1C API
11. **`ONEC_API_KEY`** или **`ONEC_AUTH_HEADER`** - авторизация

### WhatsApp (если используется):
12. **`WHATSAPP_360DIALOG_API_KEY`** или
13. **`WHATSAPP_CLOUD_API_TOKEN`** + **`WHATSAPP_CLOUD_API_PHONE_ID`**
14. **`WHATSAPP_MANAGER_PHONES`** - номера менеджеров (через запятую)

## 🔧 Что я исправил в коде:

Код теперь автоматически поддерживает оба префикса:
- `AMO_*` (оригинальный)
- `AMOCRM_*` (Railway)

Так что ваши существующие переменные `AMOCRM_*` будут работать!

## 📝 Инструкция:

1. Добавьте в Railway **только эти 3 обязательные переменные**:
   ```
   AMO_PIPELINE_ID=ваш_id_воронки
   AMO_LEAD_STATUS_ID=ваш_id_статуса
   AMO_RESPONSIBLE_USER_ID=ваш_id_пользователя
   ```

2. Остальные переменные опциональны и нужны только для расширенной функциональности.

3. Перезапустите сервис в Railway после добавления переменных.

